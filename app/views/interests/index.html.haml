.container
  .row
    .col-md-12
      = page_title(@title)
      = map(:center => { :latlng => @coordinates, :zoom => 12 }, :markers => @interests.map{|interest| { :latlng => interest.coordinates, :popup => "#{link_to interest.name, interest}" } })
      = render @interests
