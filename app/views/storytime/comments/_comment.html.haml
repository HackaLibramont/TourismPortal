.comment{:id => "comment_#{comment.id}"}
  .comment-content
    = comment.content
  .comment-details
    - if policy(comment).destroy?
      = delete_resource_link comment
        
    By: #{comment.commenter_name} · #{comment.created_at.strftime("%B %d, %Y %l:%M%P")}
